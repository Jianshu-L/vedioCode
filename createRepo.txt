echo "# NAME" >> README.md
echo "data" >> .gitignore
git init
git add README.md
git commit -m "first commit"
git branch -M main
git remote add origin git@github.com:Jianshu-L/NAME.git
git push -u origin main

conda create --name NAME python=3.8
conda activate NAME
conda install pandas numpy scipy matplotlib seaborn
conda env export > environment.yml

pip install -e .

